- content_for :javascript do
  :coffee
    $ ->
      navigator.geolocation.getCurrentPosition(setUpGoogleMap)

Existing measurements:
#main-map
%p
  = link_to 'New Speed measurement', new_speed_measurement_path
